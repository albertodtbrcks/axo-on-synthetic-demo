resources:
  apps:
    axo_app:
      name: axo-app
      description: "AXO demo: Analytics (dashboards) + Órdenes (Lakebase->Lakehouse)"
      source_code_path: ../app
      # Recurso Lakebase pendiente de permisos (grant del SP a la instancia).
      # Cuando se destrabe, agregar:
      # resources:
      #   - name: lakebase
      #     database:
      #       instance_name: axo-orders-db
      #       database_name: databricks_postgres
      #       permission: CAN_CONNECT_AND_CREATE
